<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gitpub</title>
</head>
<body>
    <h1>At foo bar</h1>
    <h2>Name: <%= drink.name %></h2>
    <img src="<%= drink.image %>">
    <h3>Price: <%= drink.price %></h3>
    <style type="text/css">
        body {
            background-color: pink;
            color: black;
        }
    </style>
    <ul>
        <% for(let i = 0; i < drink.length; i++) { %>
            <li>
                <a href="./drinks/<%= i %>"><%= drink[i].name %></a>
            </li>
       <% } %>
    </ul>
    <a href="/drinks">Back</a>
</body>
</html>